const KEY='makopet_integrated_v1';let state=JSON.parse(localStorage.getItem(KEY)||'null')||{points:25,belly:32,food:{},dress:[]};let toastTimer;function save(){localStorage.setItem(KEY,JSON.stringify(state))}function showPage(id){document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));document.getElementById(id).classList.add('active');document.querySelectorAll('.bottomNav button').forEach(b=>b.classList.remove('on'));document.getElementById('nav-'+id).classList.add('on');updateHud()}function updateHud(){document.getElementById('points').textContent=state.points;document.getElementById('belly').textContent=state.belly;document.getElementById('bellyHud').style.display=document.getElementById('room').classList.contains('active')?'inline-block':'none'}function toast(text){const t=document.getElementById('toast');t.textContent=text;t.classList.remove('hidden');clearTimeout(toastTimer);toastTimer=setTimeout(()=>t.classList.add('hidden'),1300)}function score(n){state.points+=n;save();updateHud();toast(n+'pt もらったよ')}function petTap(){toast('にこっ♪')}function buyFood(name,cost){if(state.points<cost){toast('ポイントが足りないよ');return}state.points-=cost;state.food[name]=(state.food[name]||0)+1;save();updateHud();toast(name+'を買ったよ')}function buyDress(name,cost){if(state.dress.includes(name)){toast('もう持っているよ');return}if(state.points<cost){toast('ポイントが足りないよ');return}state.points-=cost;state.dress.push(name);save();updateHud();toast(name+'を買ったよ')}function openInventory(type){const title=document.getElementById('modalTitle');const box=document.getElementById('modalItems');box.innerHTML='';if(type==='food'){title.textContent='ごはんをあげる';const entries=Object.entries(state.food).filter(([k,v])=>v>0);if(entries.length===0){box.innerHTML='<button onclick="showPage(\'shop\');closeModal()">ショップで買う</button>'}else{entries.forEach(([name,count])=>{const b=document.createElement('button');b.innerHTML=name+'<span>×'+count+'</span>';b.onclick=()=>useFood(name);box.appendChild(b)})}}else{title.textContent='きせかえ';if(state.dress.length===0){box.innerHTML='<button onclick="showPage(\'shop\');closeModal()">ショップで買う</button>'}else{state.dress.forEach(name=>{const b=document.createElement('button');b.innerHTML=name+'<span>つける</span>';b.onclick=()=>{closeModal();toast(name+'をつけたよ')};box.appendChild(b)})}}document.getElementById('inventoryModal').classList.remove('hidden')}function useFood(name){if(!state.food[name])return;state.food[name]-=1;state.belly=Math.min(50,state.belly+foodValue(name));save();updateHud();closeModal();toast(name+'を食べたよ')}function foodValue(name){return {'りんご':10,'おにぎり':15,'ドーナツ':25,'パンケーキ':30}[name]||10}function closeModal(){document.getElementById('inventoryModal').classList.add('hidden')}updateHud();
